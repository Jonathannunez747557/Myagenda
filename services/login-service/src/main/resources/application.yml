spring:
  profiles:
    active: local
  application:
    name: login-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      show-details: always