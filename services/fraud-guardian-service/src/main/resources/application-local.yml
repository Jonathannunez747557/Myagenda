spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/fraud_guardian_db
    username: myagenda
    password: myagenda

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false