spring:
  datasource:
    url: jdbc:postgresql://localhost:5445/audit_logging_db
    username: myagenda
    password: myagenda

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false