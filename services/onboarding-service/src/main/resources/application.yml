spring:
  profiles:
    active: local
  application:
    name: onboarding-service

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      show-details: always