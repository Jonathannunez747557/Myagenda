spring:
  profiles:
    active: local
  application:
    name: identity-validation-service

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      show-details: always