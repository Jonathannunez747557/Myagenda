spring:
  datasource:
    url: jdbc:postgresql://localhost:5441/authorization_db
    username: myagenda
    password: myagenda

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: false